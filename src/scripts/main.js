import {addLoginToDOM} from "./login/loginHTML";
import "../styles/main.scss";

console.log("main.js works! 👍");
// add if statement for login
// If user is logged in already don't run addLoginToDOM()
// else, run it
addLoginToDOM();